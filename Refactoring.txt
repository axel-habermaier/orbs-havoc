- simplify networking 
	* use TCP?
	* instead of classes per message, create a single Serialize function per message type that can both read and write a message
- use D3D11
- replace utility classes with .NET equivalents, e.g. 
	* bufferreader/writer -> streams
	* clock -> stopwatch
	* pool allocator -> GC
- simplify platform
	* ignore endianess
	* remove SDL
- make AssetBundle non-global
- simplify gameplay code
	* split server/client logic more cleanly